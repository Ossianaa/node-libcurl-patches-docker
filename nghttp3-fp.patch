diff -ruN '--exclude=version.h' '--exclude=buildtest' '--exclude=buildmingw' '--exclude=*.o' pristine/nghttp3-1.18.0/lib/includes/nghttp3/nghttp3.h nghttp3-1.18.0/lib/includes/nghttp3/nghttp3.h
--- pristine/nghttp3-1.18.0/lib/includes/nghttp3/nghttp3.h	2026-07-26 15:22:42.000000000 +0800
+++ nghttp3-1.18.0/lib/includes/nghttp3/nghttp3.h	2026-08-06 06:42:04.180274200 +0800
@@ -1614,6 +1614,11 @@
   nghttp3_qpack_nv *nv, uint8_t *pflags, const uint8_t *src, size_t srclen,
   int fin);
 
+typedef struct nghttp3_settings_entry {
+  uint64_t id;
+  uint64_t value;
+} nghttp3_settings_entry;
+
 /**
  * @function
  *
@@ -1899,6 +1904,15 @@
    * .. version-added:: 1.13.0
    */
   nghttp3_qpack_indexing_strat qpack_indexing_strat;
+  /**
+   * :member:`fp_iv` is the optional fingerprint SETTINGS list.  If
+   * :member:`fp_ivlen` is nonzero, the SETTINGS frame is built
+   * exclusively from these entries, in the given order, instead of the
+   * default SETTINGS.  The caller must keep the memory pointed to by
+   * :member:`fp_iv` valid for the lifetime of the connection.
+   */
+  nghttp3_settings_entry *fp_iv;
+  size_t fp_ivlen;
 } nghttp3_settings;
 
 #define NGHTTP3_PROTO_SETTINGS_V1 1
diff -ruN '--exclude=version.h' '--exclude=buildtest' '--exclude=buildmingw' '--exclude=*.o' pristine/nghttp3-1.18.0/lib/nghttp3_conn.c nghttp3-1.18.0/lib/nghttp3_conn.c
--- pristine/nghttp3-1.18.0/lib/nghttp3_conn.c	2026-07-26 15:22:42.000000000 +0800
+++ nghttp3-1.18.0/lib/nghttp3_conn.c	2026-08-06 06:40:45.500729400 +0800
@@ -332,6 +332,20 @@
   nghttp3_qpack_encoder_set_indexing_strat(&conn->qenc,
                                            settings->qpack_indexing_strat);
 
+  /* Fingerprint profile: if the local settings advertise a QPACK
+     dynamic table capacity, size the encoder accordingly (as Chrome
+     does) so the encoder stream carries the capacity instruction and
+     stays consistent with the advertised SETTINGS. */
+  if (settings->qpack_max_dtable_capacity) {
+    size_t enc_cap = settings->qpack_max_dtable_capacity;
+
+    if (settings->qpack_encoder_max_dtable_capacity &&
+        enc_cap > settings->qpack_encoder_max_dtable_capacity) {
+      enc_cap = settings->qpack_encoder_max_dtable_capacity;
+    }
+    nghttp3_qpack_encoder_set_max_dtable_capacity(&conn->qenc, enc_cap);
+  }
+
   nghttp3_pq_init(&conn->qpack_blocked_streams, ricnt_less, mem);
 
   for (i = 0; i < NGHTTP3_URGENCY_LEVELS; ++i) {
diff -ruN '--exclude=version.h' '--exclude=buildtest' '--exclude=buildmingw' '--exclude=*.o' pristine/nghttp3-1.18.0/lib/nghttp3_frame.h nghttp3-1.18.0/lib/nghttp3_frame.h
--- pristine/nghttp3-1.18.0/lib/nghttp3_frame.h	2026-07-26 15:22:42.000000000 +0800
+++ nghttp3-1.18.0/lib/nghttp3_frame.h	2026-08-06 06:40:45.516733500 +0800
@@ -82,11 +82,6 @@
 #define NGHTTP3_H2_SETTINGS_ID_INITIAL_WINDOW_SIZE 0x4U
 #define NGHTTP3_H2_SETTINGS_ID_MAX_FRAME_SIZE 0x5U
 
-typedef struct nghttp3_settings_entry {
-  uint64_t id;
-  uint64_t value;
-} nghttp3_settings_entry;
-
 typedef struct nghttp3_frame_settings {
   uint64_t type;
   size_t niv;
diff -ruN '--exclude=version.h' '--exclude=buildtest' '--exclude=buildmingw' '--exclude=*.o' pristine/nghttp3-1.18.0/lib/nghttp3_stream.c nghttp3-1.18.0/lib/nghttp3_stream.c
--- pristine/nghttp3-1.18.0/lib/nghttp3_stream.c	2026-07-26 15:22:42.000000000 +0800
+++ nghttp3-1.18.0/lib/nghttp3_stream.c	2026-08-06 06:42:19.674431300 +0800
@@ -341,6 +341,40 @@
   const nghttp3_settings *local_settings = infr->local_settings;
   uint64_t payloadlen;
 
+  if (local_settings->fp_ivlen) {
+    /* Fingerprint profile: send exactly the configured SETTINGS, in
+       wire order, instead of the defaults. */
+    size_t i;
+
+    if (local_settings->fp_ivlen > 15) {
+      return NGHTTP3_ERR_INVALID_ARGUMENT;
+    }
+
+    fr.niv = local_settings->fp_ivlen;
+    for (i = 0; i < local_settings->fp_ivlen; ++i) {
+      ents[i] = (nghttp3_settings_entry){
+        .id = local_settings->fp_iv[i].id,
+        .value = local_settings->fp_iv[i].value,
+      };
+    }
+
+    len = nghttp3_frame_write_settings_len(&payloadlen, &fr);
+
+    rv = nghttp3_stream_ensure_chunk(stream, len);
+    if (rv != 0) {
+      return rv;
+    }
+
+    chunk = nghttp3_stream_get_chunk(stream);
+    nghttp3_typed_buf_shared_init(&tbuf, chunk);
+
+    chunk->last = nghttp3_frame_write_settings(chunk->last, &fr, payloadlen);
+
+    tbuf.buf.last = chunk->last;
+
+    return nghttp3_stream_outq_add(stream, &tbuf);
+  }
+
   ents[0] = (nghttp3_settings_entry){
     .id = NGHTTP3_SETTINGS_ID_MAX_FIELD_SECTION_SIZE,
     .value = local_settings->max_field_section_size,
