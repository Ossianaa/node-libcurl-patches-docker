diff -ruN '--exclude=version.h' '--exclude=buildtest' '--exclude=buildmingw' '--exclude=*.o' pristine/nghttp3-1.0.0/lib/includes/nghttp3/nghttp3.h nghttp3-1.0.0/lib/includes/nghttp3/nghttp3.h
--- pristine/nghttp3-1.0.0/lib/includes/nghttp3/nghttp3.h	2023-10-15 20:21:34.000000000 +0800
+++ nghttp3-1.0.0/lib/includes/nghttp3/nghttp3.h	2026-08-05 09:48:01.650402100 +0800
@@ -1607,6 +1607,11 @@
 #define NGHTTP3_SETTINGS_V1 1
 #define NGHTTP3_SETTINGS_VERSION NGHTTP3_SETTINGS_V1
 
+typedef struct nghttp3_settings_entry {
+  uint64_t id;
+  uint64_t value;
+} nghttp3_settings_entry;
+
 /**
  * @struct
  *
@@ -1652,6 +1657,15 @@
    * Datagrams (see :rfc:`9297`).
    */
   uint8_t h3_datagram;
+  /**
+   * :member:`fp_iv` is the optional fingerprint SETTINGS list.  If
+   * :member:`fp_ivlen` is nonzero, the SETTINGS frame is built
+   * exclusively from these entries, in the given order, instead of the
+   * default SETTINGS.  The caller must keep the memory pointed to by
+   * :member:`fp_iv` valid for the lifetime of the connection.
+   */
+  nghttp3_settings_entry *fp_iv;
+  size_t fp_ivlen;
 } nghttp3_settings;
 
 /**
diff -ruN '--exclude=version.h' '--exclude=buildtest' '--exclude=buildmingw' '--exclude=*.o' pristine/nghttp3-1.0.0/lib/nghttp3_conn.c nghttp3-1.0.0/lib/nghttp3_conn.c
--- pristine/nghttp3-1.0.0/lib/nghttp3_conn.c	2023-10-15 20:21:34.000000000 +0800
+++ nghttp3-1.0.0/lib/nghttp3_conn.c	2026-08-05 11:14:58.116353700 +0800
@@ -267,6 +267,20 @@
     goto qenc_init_fail;
   }
 
+  /* Fingerprint profile: if the local settings advertise a QPACK
+     dynamic table capacity, size the encoder accordingly (as Chrome
+     does) so the encoder stream carries the capacity instruction and
+     stays consistent with the advertised SETTINGS. */
+  if (settings->qpack_max_dtable_capacity) {
+    size_t enc_cap = settings->qpack_max_dtable_capacity;
+
+    if (settings->qpack_encoder_max_dtable_capacity &&
+        enc_cap > settings->qpack_encoder_max_dtable_capacity) {
+      enc_cap = settings->qpack_encoder_max_dtable_capacity;
+    }
+    nghttp3_qpack_encoder_set_max_dtable_capacity(&conn->qenc, enc_cap);
+  }
+
   nghttp3_pq_init(&conn->qpack_blocked_streams, ricnt_less, mem);
 
   for (i = 0; i < NGHTTP3_URGENCY_LEVELS; ++i) {
diff -ruN '--exclude=version.h' '--exclude=buildtest' '--exclude=buildmingw' '--exclude=*.o' pristine/nghttp3-1.0.0/lib/nghttp3_frame.h nghttp3-1.0.0/lib/nghttp3_frame.h
--- pristine/nghttp3-1.0.0/lib/nghttp3_frame.h	2023-10-15 20:21:34.000000000 +0800
+++ nghttp3-1.0.0/lib/nghttp3_frame.h	2026-08-05 09:51:34.632255900 +0800
@@ -80,11 +80,6 @@
 #define NGHTTP3_H2_SETTINGS_ID_INITIAL_WINDOW_SIZE 0x4
 #define NGHTTP3_H2_SETTINGS_ID_MAX_FRAME_SIZE 0x5
 
-typedef struct nghttp3_settings_entry {
-  uint64_t id;
-  uint64_t value;
-} nghttp3_settings_entry;
-
 typedef struct nghttp3_frame_settings {
   nghttp3_frame_hd hd;
   size_t niv;
diff -ruN '--exclude=version.h' '--exclude=buildtest' '--exclude=buildmingw' '--exclude=*.o' pristine/nghttp3-1.0.0/lib/nghttp3_stream.c nghttp3-1.0.0/lib/nghttp3_stream.c
--- pristine/nghttp3-1.0.0/lib/nghttp3_stream.c	2023-10-15 20:21:34.000000000 +0800
+++ nghttp3-1.0.0/lib/nghttp3_stream.c	2026-08-05 12:02:19.615379900 +0800
@@ -345,6 +345,43 @@
   nghttp3_settings *local_settings = frent->aux.settings.local_settings;
 
   fr.settings.hd.type = NGHTTP3_FRAME_SETTINGS;
+
+  if (local_settings->fp_ivlen) {
+    /* Fingerprint profile: send exactly the configured SETTINGS, in order.
+       Note: use a pointer into the iv array (as the default path does);
+       direct `settings.iv[i]` member access beyond the declared iv[1] is
+       undefined behaviour that -O2 optimizers may miscompile. */
+    size_t i;
+    nghttp3_settings_entry *iv = &fr.settings.iv[0];
+
+    if (local_settings->fp_ivlen > 15) {
+      return NGHTTP3_ERR_INVALID_ARGUMENT;
+    }
+
+    fr.settings.niv = local_settings->fp_ivlen;
+    for (i = 0; i < local_settings->fp_ivlen; ++i) {
+      iv[i].id = local_settings->fp_iv[i].id;
+      iv[i].value = local_settings->fp_iv[i].value;
+    }
+
+    len = nghttp3_frame_write_settings_len(&fr.settings.hd.length,
+                                           &fr.settings);
+
+    rv = nghttp3_stream_ensure_chunk(stream, len);
+    if (rv != 0) {
+      return rv;
+    }
+
+    chunk = nghttp3_stream_get_chunk(stream);
+    typed_buf_shared_init(&tbuf, chunk);
+
+    chunk->last = nghttp3_frame_write_settings(chunk->last, &fr.settings);
+
+    tbuf.buf.last = chunk->last;
+
+    return nghttp3_stream_outq_add(stream, &tbuf);
+  }
+
   fr.settings.niv = 3;
   iv = &fr.settings.iv[0];
 
